{
  "version": 2,
  "builds": [
    { "src": "/backend/**", "use": "@vercel/node" },
    { "src": "/frontend/**", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend/$1" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}